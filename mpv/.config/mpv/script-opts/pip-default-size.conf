# Auto-apply a PiP size preset when a video finishes loading.
enabled=yes

# Preset passed to hypr-mpv-pip-size: half | normal | double
mode=normal

# Delay before applying resize (seconds).
delay=0.20
