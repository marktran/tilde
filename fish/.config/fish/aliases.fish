abbr -a b brew
alias c 'calc -d'
abbr -a g git
alias ip 'dig +short myip.opendns.com @resolver1.opendns.com'
alias l 'ls'
alias ll 'ls -l'
abbr -a n 'nvim'
abbr -a s sesh
abbr -a t tmux
abbr -a vi nvim

type -q gcal; and alias cal gcal
