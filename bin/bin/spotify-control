#!/bin/bash
# Wrapper for controlling Spotify via playerctl from keyd (running as root)
# Usage: spotify-control <previous|next|play-pause>

sudo -u mark DBUS_SESSION_BUS_ADDRESS=unix:path=/run/user/1000/bus \
  swayosd-client --player=spotify --playerctl "$1"
