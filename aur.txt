# AUR packages for bootstrapping (curated list, not a full snapshot).
#
# These are NOT in the official repos, so plain pacman cannot install them.
# Install with an AUR helper:
#   paru -S --needed - < aur.txt
#   # or: yay -S --needed - < aur.txt
#
# Official-repo packages live in packages.txt (install with pacman).
#
# Regenerate from this machine's explicitly-installed foreign (AUR) packages:
#   pacman -Qqem > aur.txt   (then re-add these comments)
# List what's installed from AUR but missing here:
#   comm -23 <(pacman -Qqem | sort) <(grep -v '^#' aur.txt | sort -u)

brave-origin-beta-bin
google-cloud-cli
sesh-bin
