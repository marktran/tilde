{ ... }:

{
  # Host-specific user config for the ThinkPad X1 Carbon Gen 13 on Omarchy.
  # Keep privileged /etc files in system/ until this host is managed by NixOS
  # or another explicit system-level tool.
}
